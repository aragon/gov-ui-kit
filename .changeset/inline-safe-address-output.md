---
"@aragon/gov-ui-kit": patch
---

Render `AddressOutput` and `Clipboard` as inline-safe spans, apply `AddressOutput` `className` to the component root, and add `InteractiveAncestorContext` so wrapper components set `hasInteractiveAncestor` once for a whole subtree.
