---
"@aragon/gov-ui-kit": patch
---

Render `AddressOutput` and `Clipboard` as inline-safe spans, keep passive address labels centered in control rows, apply `AddressOutput` `className` to the component root, and add `InteractiveAncestorContext` so wrapper components—including `Wallet`—avoid nested address controls.
